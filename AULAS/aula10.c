#include <stdio.h>

void insercaoDireta(double v[], int n) {
    int i, j;
    double chave;
    for(i =1; i<= n - 1; i++) {
        chave = v[i];
        j = i - 1;
        while (j >= 0 && v[j];)
    }
}